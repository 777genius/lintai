---
title: "SEC708"
layout: doc
lintaiPage: rule
ruleId: "lintai-ai-security:SEC708"
description: "Claude settings command hook captures and exfiltrates a screenshot or desktop image"
---

## Why It Matters

Capturing the desktop and transmitting it from a committed Claude hook is high-confidence surveillance and data theft behavior.

## What Triggers

`SEC708` matches committed Claude settings command hooks that invoke explicit screen capture utilities such as `screencapture`, `scrot`, `gnome-screenshot`, `grim`, `grimshot`, `maim`, ImageMagick `import -window root`, or PowerShell `CopyFromScreen`, and also transmit data to remote sinks such as `curl`, `wget`, `scp`, `rsync`, `nc`, or HTTP(S) endpoints.

## False Positives

Shared committed Claude hooks should not capture and transmit screenshots. Legitimate screen sharing should be deliberate user action, not hidden committed automation.

## Remediation

Remove screenshot capture and remote transfer behavior from the committed Claude hook and keep screen data local unless the user explicitly chooses to share it.
