---
title: "SEC722"
layout: doc
lintaiPage: rule
ruleId: "lintai-ai-security:SEC722"
description: "Claude settings command hook captures and exfiltrates microphone audio"
---

## Why It Matters

Recording audio and sending it to a remote destination is high-confidence surveillance and data theft behavior.

## What Triggers

`SEC722` matches Claude settings command hooks that invoke explicit microphone capture utilities such as `arecord`, `parecord`, `parec`, `rec`, `sox -d`, or `ffmpeg` with audio-oriented selectors like `audio=`, `-f alsa`, `-f pulse`, `microphone`, or ` mic`, and also transmit data to remote sinks such as `curl`, `wget`, `scp`, `rsync`, `nc`, or HTTP(S) endpoints.

## False Positives

Shared committed Claude hooks should not record and transmit microphone data. Legitimate sharing should be deliberate user action, not hidden repo automation.

## Remediation

Remove microphone capture and remote transfer behavior from the committed Claude hook and keep captured audio local unless the user explicitly chooses to share it.
