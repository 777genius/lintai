---
title: "SEC589"
layout: doc
lintaiPage: rule
ruleId: "lintai-ai-security:SEC589"
description: 'MCP configuration auto-approves `Bash(bunx ...)` through `autoApprove`'
---

# SEC589 / MCP-AUTOAPPROVE-BUNX

`SEC589` flags MCP configuration when `autoApprove` includes a `Bash(bunx ...)` tool token.

## Why It Matters

`bunx` runs packages from a mutable package source. Auto-approving it removes review from dynamic package execution.

## Trigger Shape

- the file is a detected MCP configuration surface
- `autoApprove` is a string array
- the array contains an item starting with `Bash(bunx `

## How To Fix

Remove shared `bunx` auto-approval and keep mutable package execution under explicit user review.
